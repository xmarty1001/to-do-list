# Todo List Manager

## 📌 Project Title
**Todo List Manager** — A simple command-line application to organize and track tasks.

## 📘 Overview
This project is a beginner‑friendly Python CLI program that helps users manage their daily tasks.  
You can add new tasks, mark them as completed, delete tasks, and store everything in a CSV file so your progress is never lost.

## ✨ Features
- Add a task with a title
- View all existing tasks
- Mark a task as completed
- Delete a task
- Data persistence using CSV file
- Automatically assigns unique task IDs

## 🛠️ Technologies / Tools Used
- **Python 3.x**
- **CSV** for data storage
- Built‑in Python modules only (no external dependencies)

## 🚀 Installation & Run Instructions

1️⃣ Make sure Python is installed:  
```bash
python --version
```

2️⃣ Navigate to the project folder:  
```bash
cd todo_list_manager_extracted
```

3️⃣ Run the application:  
```bash
python main.py
```

> A file called `tasks.csv` will be created automatically after saving tasks.

## 🧪 Testing Instructions
To test the functionality:

1. Run the program and select **Add Task**
2. Add 2–3 tasks
3. Select **View Tasks** to confirm they appear
4. Mark a task as completed using its ID
5. Delete a task
6. Exit with **Save and Exit**
7. Restart the program to verify your saved tasks load correctly

---

✨ *Feel free to extend features like due dates, priority levels, or migrating to a database!*